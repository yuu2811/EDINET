# EDINET API Subscription Key (required)
# Get yours at: https://disclosure2dl.edinet-fsa.go.jp/guide/static/disclosure/WZEK0110.html
EDINET_API_KEY=your_subscription_key_here

# Polling interval in seconds (default: 60)
POLL_INTERVAL=60

# Database URL (default: SQLite)
DATABASE_URL=sqlite+aiosqlite:///./edinet_monitor.db

# Server settings
HOST=0.0.0.0
PORT=8000

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# CORS allowed origins (comma-separated, default: "*" for all)
# Example: ALLOWED_ORIGINS=https://myapp.example.com,http://localhost:3000
ALLOWED_ORIGINS=*

# EDINET API base URL (default: official v2 endpoint)
# EDINET_API_BASE=https://api.edinet-fsa.go.jp/api/v2

# Stock data cache TTL in seconds (default: 1800 = 30 minutes)
# STOCK_CACHE_TTL=1800
